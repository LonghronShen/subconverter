--- a/source/socketBase/socketBase.h
+++ b/source/socketBase/socketBase.h
@@ -1,12 +1,24 @@
 #ifndef SOCKETBASE_H
 #define SOCKETBASE_H
 
-#include <arpa/inet.h>
 #include <future>
 #include <iostream>
+#include <thread>
+
+#ifdef WIN32
+#include <winsock2.h>
+#include <ws2tcpip.h>
+#ifndef MSG_DONTWAIT
+#define MSG_DONTWAIT 0
+#endif
+#ifndef close
+#define close closesocket
+#endif
+#else
+#include <arpa/inet.h>
 #include <netinet/in.h>
 #include <sys/socket.h>
-#include <thread>
+#endif
 
 #ifndef SINGLE_HEADER
 #include "../connectionBase/connectionBase.h"
--- a/source/connectionBase/connectionBase.h
+++ b/source/connectionBase/connectionBase.h
@@ -5,9 +5,23 @@
 #include <future>
 #include <iostream>
 #include <list>
-#include <sys/socket.h>
 #include <thread>
+
+#ifdef WIN32
+#include <winsock2.h>
+#ifndef MSG_DONTWAIT
+#define MSG_DONTWAIT 0
+#endif
+#ifdef DELETE
+#undef DELETE
+#endif
+#ifndef close
+#define close closesocket
+#endif
+#else
+#include <sys/socket.h>
 #include <unistd.h>
+#endif
 
 #ifndef SINGLE_HEADER
 #include "../packet/packet.h"
--- a/source/tcpSocket/tcpSocket.h
+++ b/source/tcpSocket/tcpSocket.h
@@ -1,12 +1,24 @@
 #ifndef TCPSOCKET_H
 #define TCPSOCKET_H
 
-#include <arpa/inet.h>
 #include <future>
 #include <iostream>
+#include <thread>
+
+#ifdef WIN32
+#include <winsock2.h>
+#include <ws2tcpip.h>
+#ifndef MSG_DONTWAIT
+#define MSG_DONTWAIT 0
+#endif
+#ifndef close
+#define close closesocket
+#endif
+#else
+#include <arpa/inet.h>
 #include <netinet/in.h>
 #include <sys/socket.h>
-#include <thread>
+#endif
 
 #ifndef SINGLE_HEADER
 #include "../socketBase/socketBase.h"
--- a/source/tcpSocket/tcpSocket.cpp
+++ b/source/tcpSocket/tcpSocket.cpp
@@ -4,7 +4,11 @@
   address.sin_family = AF_INET;
 
   address.sin_port = htons(listenPort);
+#ifdef WIN32
+  inet_pton(AF_INET, listenAddress, &address.sin_addr);
+#else
   inet_aton(listenAddress, (in_addr*)&address.sin_addr.s_addr);
+#endif
 }
 
 kleins::tcpSocket::~tcpSocket() {
@@ -23,14 +27,23 @@
 
 std::future<bool> kleins::tcpSocket::init() {
   auto init_async = [this]() {
+    opt = 1;
     socketfd = socket(AF_INET, SOCK_STREAM, 0);
 
+#ifdef WIN32
+    if (socketfd == INVALID_SOCKET) {
+#else
     if (!socketfd) {
+#endif
       std::cerr << "Error creating socket file descriptor" << std::endl;
       return false;
     }
 
+#ifdef WIN32
+    if (setsockopt(socketfd, SOL_SOCKET, SO_REUSEADDR | SO_KEEPALIVE, (const char*)&opt, sizeof(opt))) {
+#else
     if (setsockopt(socketfd, SOL_SOCKET, SO_REUSEADDR | SO_REUSEPORT | SO_KEEPALIVE, &opt, sizeof(opt))) {
+#endif
       std::cerr << "Error setting socket opts" << std::endl;
       return false;
     }
@@ -46,6 +59,11 @@
 
     addrlen = sizeof(address);
 
+#ifdef WIN32
+    u_long nonblocking = 1;
+    ioctlsocket(socketfd, FIONBIO, &nonblocking);
+#endif
+
     return true;
   };
 
--- a/source/tcpConnection/tcpConnection.h
+++ b/source/tcpConnection/tcpConnection.h
@@ -4,9 +4,17 @@
 #include <future>
 #include <iostream>
 #include <list>
-#include <sys/socket.h>
 #include <thread>
+
+#ifdef WIN32
+#include <winsock2.h>
+#ifndef close
+#define close closesocket
+#endif
+#else
+#include <sys/socket.h>
 #include <unistd.h>
+#endif
 
 #ifndef SINGLE_HEADER
 #include "../connectionBase/connectionBase.h"
--- a/source/tcpConnection/tcpConnection.cpp
+++ b/source/tcpConnection/tcpConnection.cpp
@@ -12,8 +12,13 @@
 
 bool kleins::tcpConnection::getAlive() {
   int error = 0;
+#ifdef WIN32
+  int len = sizeof(error);
+  int retval = getsockopt(connectionfd, SOL_SOCKET, SO_ERROR, (char*)&error, &len);
+#else
   socklen_t len = sizeof(error);
   int retval = getsockopt(connectionfd, SOL_SOCKET, SO_ERROR, &error, &len);
+#endif
 
   return (error | retval) == 0;
 }
@@ -26,7 +31,7 @@
 
   if (packetBuffer->size == -1) {
     delete packetBuffer;
-    usleep(20000);
+    std::this_thread::sleep_for(std::chrono::microseconds(20000));
 
     if (getTimeout()) {
       close_socket();
--- a/source/connectionBase/connectionBase.cpp
+++ b/source/connectionBase/connectionBase.cpp
@@ -16,7 +16,7 @@
 void kleins::connectionBase::ownTickLoop(connectionBase* connection) {
   while (connection->getAlive()) {
     connection->tick();
-    usleep(2000);
+    std::this_thread::sleep_for(std::chrono::microseconds(2000));
   }
 }
 
--- a/source/httpServer/httpServer.cpp
+++ b/source/httpServer/httpServer.cpp
@@ -197,7 +197,7 @@
   fileLookup.insert(std::make_pair(uri, filedata));
 
   on(GET, uri, [this](httpParser* parser) {
-    std::string extension = std::filesystem::path(parser->path).extension();
+    std::string extension = std::filesystem::path(parser->path).extension().string();
     std::string mimetype = mimeLookup[extension];
 
     if (mimeLookup.find(extension) != mimeLookup.end()) {
@@ -218,12 +218,12 @@
     if (!p.is_regular_file())
       continue;
 
-    std::string filepath = p.path();
+    std::string filepath = p.path().string();
 
     if (p.path().filename() == indexFile) {
-      serve(((std::string)p.path().parent_path()).append("/").substr(path.length()), p.path());
+      serve(p.path().parent_path().string().append("/").substr(path.length()), p.path().string());
 
-      std::string incompletePath = ((std::string)p.path().parent_path()).substr(path.length());
+      std::string incompletePath = p.path().parent_path().string().substr(path.length());
       if (incompletePath.length() != 0) {
         on(GET, incompletePath, [](httpParser* parser) {
           std::string locationHeader = "Location: " + parser->path + "/";
@@ -231,7 +231,7 @@
         });
       }
     } else {
-      serve(filepath.substr(path.length()), p.path());
+      serve(filepath.substr(path.length()), p.path().string());
     }
   }
 }
@@ -261,7 +261,7 @@
       it++;
     }
 
-    usleep(5000000);
+    std::this_thread::sleep_for(std::chrono::microseconds(5000000));
   }
 }
 
